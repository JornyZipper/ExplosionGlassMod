package bigworld.impl;

import bigworld.InstrumentationAPI;

/**
 * Example stub to show how `explosionGlass` could implement the API.
 * Real implementation should live in the explosionGlass mod.
 */
public class ExplosionGlassInstrumentationStub implements InstrumentationAPI {
    @Override
    public void instrumentExplosionGlass(Object target) {
        // example: attach optimized handlers or replace heavy objects
    }
}
